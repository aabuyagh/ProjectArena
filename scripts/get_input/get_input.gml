//Set the controller being used
dvc = argument0;

//Set the vertical and horizontal values of the left joystick
verticalMovement = gamepad_axis_value(dvc,gp_axislv);
horizontalMovement = gamepad_axis_value(dvc,gp_axislh);

